def print_even_length_substring(input_string):
    words=input string.split()
    even.length_substrings=[words for word in words if len(word)%2==0]
    print(''.join(even_length_substrings))
input_string="the sun rises in the east"
print_even_length_substring(input_string)
